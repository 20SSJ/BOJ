#include <iostream>
#include <list>
using namespace std;

int main(){
    int N, K;
    cin >> N >> K;
    list<int> L;
    list<int> ::iterator t;
    for(int i = 1; i < N + 1; i++) L.push_back(i);
    
    return 0;
}